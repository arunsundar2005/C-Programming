#include <stdio.h>       // For printf
#include "myHeader.h"    // Include the user-defined header file

int main() {
    
    printf("The cube of 3 is %d\n", Cube(3));  // Use the Cube macro
    return 0;
}
